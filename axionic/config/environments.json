{
    "dev": {
        "baseUrl": "https://wapi.discretal.com",
        "auth": {
            "tokenType": "Bearer",
            "token": ""  // Token will be set after login
        }
    },
    "staging": {
        "baseUrl": "https://wapis.discretal.com",
        "auth": {
            "tokenType": "Bearer",
            "token": ""  // Token will be set after login
        }
    }
}